name: asd_intervention
description: "ASD Intervention System - Social Skills Adventure mobile app"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Game engine
  flame: ^1.22.0
  flame_audio: ^2.11.0

  # Local storage
  sqflite: ^2.3.0
  sqflite_common_ffi_web: ^1.0.0
  path_provider: ^2.1.0
  path: ^1.9.0
  shared_preferences: ^2.2.0

  # State management
  flutter_riverpod: ^3.0.0

  # Networking
  dio: ^5.4.0
  connectivity_plus: ^5.0.0

  # Utilities
  uuid: ^4.0.0
  cupertino_icons: ^1.0.8

  # Secure token storage
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  sqflite_common_ffi: ^2.3.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - lib/assets/avatar/
    - lib/assets/avatars/
    - lib/assets/background/
    - lib/assets/emotion/
    - lib/assets/item/sweets/
    - lib/assets/item/toys/
    - lib/assets/audio/sharing/
    - lib/assets/audio/patience/
    - lib/assets/UI/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
